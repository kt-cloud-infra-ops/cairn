---
triggers:                        # 이 코드 편집 시 로드 + drift 감지 기준
  - "{Controller}.java"
  - "{Service}Impl.java"
  - "{sqlmap}.xml"
last_verified: "{YYYY-MM-DD}"    # triggers 코드와 마지막 대조일 (stale 판정 기준)
maintainer: "{owner 이메일ID}"   # 분산 소유 — 이 그릇 담당자 (팀원↑ 시 중앙 병목 방지)
---

# {모듈} 도메인 에이전트

> 서비스: {service} · 모듈: {module}
> **살아있는 스펙 문서.** 코드 변경 시 AI가 먼저 읽고, 작업 후 새 결정을 적립한다.
> 목적: owner 부재에도 이 문서만으로 같은 수준의 판단이 재현되게 한다.

## 준수 규칙 (What — 어기면 사고)

<!-- 코드 편집 시 반드시 지킬 불변 규칙. 짧고 단정적으로. -->
- 

## 결정 이력 (Why — 왜 이렇게 설계했나)

<!--
가장 중요한 섹션. 인수인계·휴직 시 "모르고 바꾸면 터지는" 지점.
새 결정을 내릴 때마다 한 줄씩 적립한다.
-->

| 결정 | 배경/제약 | 선택 이유 | 바꾸면 안 되는 경계 (위험) |
|------|----------|----------|--------------------------|
| _예: A·B 모듈 분리_ | _공유 권한 필터_ | _결합 시 필터 우회 위험_ | _통합하면 권한 검증 깨짐_ |

## 판단 시나리오 (How to decide — 새 상황 판단 기준)

<!--
SOP가 못 담는 "예외 판단". owner라면 이렇게 결정한다는 기준.
"이런 요청이 오면 무엇을 확인하고 어떻게 결론내는가".
-->

| 상황 / 요청 | 먼저 확인할 것 | 판단 기준 | 기본 결론 |
|-------------|--------------|----------|----------|
| _예: 새 상태값 추가 요청_ | _기존 상태 흐름과 충돌?_ | _하위 호환 가능 여부_ | _별도 흐름 분리_ |

## 요구사항 이력 (확정 스펙 축적)

<!-- 작업하며 확인·확정된 스펙을 날짜와 함께 누적. -->
- 

## 관련 문서

- 결정(ADR): `docs/decisions/`
- 피처 설계: `docs/features/`
- 운영 SOP: `base/services/{service-path}/sop/`
