# Architecture Document

> PRD의 기능 요구사항을 기술적으로 어떻게 구현할지 정의한다.
> PRD가 "무엇을"이라면 Architecture는 "어떤 구조로"를 정의한다.

## 기본 정보

| 항목 | 내용 |
|------|------|
| PRD 문서 | [feature-prd.md 경로] |
| Jira 티켓 | [TECHIOPS26-XXX] |
| 상태 | `draft` / `review` / `approved` |

## 시스템 구조

### 컴포넌트 다이어그램

```
[호출자] → [Controller] → [Service] → [Repository] → [DB]
                                    → [External API]
```

### 변경 대상 컴포넌트

| 컴포넌트 | 패키지/경로 | 변경 유형 | 설명 |
|---------|-----------|----------|------|
| [Controller] | [패키지 경로] | 신규/수정 | |
| [Service] | [패키지 경로] | 신규/수정 | |
| [Repository] | [패키지 경로] | 신규/수정 | |
| [Entity] | [패키지 경로] | 신규/수정 | |

## 데이터 모델

### ERD (변경 부분)

```
[테이블A] 1──N [테이블B]
              1──1 [테이블C]
```

### 테이블 변경 상세

| 테이블 | 변경 | 컬럼 | 타입 | 제약 | 비고 |
|--------|------|------|------|------|------|
| [테이블명] | ADD/ALTER/DROP | [컬럼명] | [타입] | [제약] | |

### 인덱스 전략

| 테이블 | 인덱스명 | 컬럼 | 유형 | 사유 |
|--------|---------|------|------|------|
| | | | BTREE/GIN/HASH | |

## API 상세 설계

### 시퀀스 다이어그램

```
Client → Controller → Service → Repository → DB
                            → ExternalAPI
         ←  Response  ←
```

### 트랜잭션 경계

| 메서드 | 트랜잭션 | 격리 수준 | 비고 |
|--------|---------|----------|------|
| [메서드명] | @Transactional | READ_COMMITTED | |

### 에러 처리 전략

| 예외 상황 | 처리 방법 | HTTP 상태 | 에러 코드 |
|----------|----------|----------|----------|
| [상황] | [방법] | [상태] | [코드] |

## 외부 연동

> CRITICAL: Spec-First — 실제 API 스펙 확인 후 작성. 유추 금지.

### [연동 대상명]

| 항목 | 내용 |
|------|------|
| API 문서 | [URL 또는 파일 경로] |
| 인증 방식 | [Bearer / API Key / Basic] |
| Base URL | [확인된 URL] |
| 타임아웃 | connect: [N]ms, read: [N]ms |
| 재시도 정책 | [Transient 에러만 / Exponential Backoff] |
| 서킷브레이커 | [적용 여부 + 임계값] |

**실제 응답 샘플** (확인 완료):
```json
{
  "확인된 실제 응답 구조"
}
```

## 성능 고려사항

| 항목 | 예상 | 대응 |
|------|------|------|
| 데이터 규모 | [현재/1년후] | |
| 동시 요청 | [예상 TPS] | |
| 쿼리 성능 | [대상 쿼리 + EXPLAIN 결과] | |
| 캐시 전략 | [적용 여부 + TTL] | |

## 보안 고려사항

> agents/rules-on-demand/security.md 체크리스트 수행

| 항목 | 적용 | 비고 |
|------|------|------|
| 입력 검증 | | |
| SQL Injection 방지 | | |
| XSS 방지 | | |
| 인증/인가 | | |
| 시크릿 관리 (Jasypt) | | |
| 파일 업로드 (Tika) | 해당없음 / 적용 | |

## 의사결정 기록

| # | 결정 | 대안 | 선택 사유 |
|---|------|------|----------|
| 1 | [결정 내용] | [검토한 대안] | [선택 근거] |
